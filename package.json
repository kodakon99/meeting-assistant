{
  "name": "meeting-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "AI meeting assistant — audio to living task graph",
  "scripts": {
    "dev": "node ./node_modules/concurrently/dist/bin/concurrently.js -n client,server -c cyan,magenta \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
